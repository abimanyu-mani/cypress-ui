stages:
  - test

run-tests:
  stage: test
  image: cypress/included:15.4.0
  script:
    - echo "Running all Cypress tests..."
    - npx cypress run --browser chrome --headless --spec 'cypress/e2e/tests/**/*.cy.js'