stages:
  - test

run-tests:
  stage: test
  image: cypress/included:15.4.0
  script:
    - npm ci
    - npm run test:all
  artifacts:
    when: always
    paths:
      - cypress/screenshots
      - cypress/videos
  